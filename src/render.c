#include "render.h"



bool Process_Execute(Process *pr,Explorer *exp)
{

    switch(pr->type)
    {

        case PROCESS_MOVE:
            GameVector v= pr->ProcessValue.Move;
            exp->
    
        case PROCESS_SIZE:


        case PROCESS_TRANSPARENCY:



    }
    return true;
}
